#include "main.h"
#include <stdio.h>
#include <ctype.h>
/**
  * _isupper - fundction
  * @c: declare
  * Return: anyway
  */
int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
else
{
return (0);
}
}
