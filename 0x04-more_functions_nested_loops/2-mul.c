#include "main.h"
#include <stdio.h>
#include <ctype.h>
/**
 * mul - multubliy to number
 * @a: first number
 * @b: second number
 * Return: return result
 */
int mul(int a, int b)
{
int x;
return (x = a * b);
}
