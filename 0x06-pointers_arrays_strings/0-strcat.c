#include "main.h"
#include <string.h>
/**
 * _strcat - concatinate two string
 * @dest: destinartion
 * @src: course
 * Return: the dest
 */
char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
