#include <stdio.h>
#include <string.h>

char *leet(char *s)
{
int i, x;
    char leetMap[26][5] = {
        {'4', '@', 'a', '/\\', '^'},
        {'8', '|3', 'b', '13'},
        {'(', '<', 'c', '{', '['},
        {'D', 'd', '|}'},
        {'3', '[-', 'e', '3', '&'},
        {'F', '|=', 'f', '|#', 'ph'},
        {'6', '(_+', 'g', '[,', '&'},
        {'#', '|-|', 'h', '|-|', '[-]'},
        {'!', 'i', '1', '|'},
        {'_|', 'J', 'j', '_|'},
        {'|<', '|<', 'k', '/<', '|{'},
        {'1', '|_', 'l', '|', '|' },
        {'M', '|\/|', 'm', '|v|', '^^'},
        {'N', '|\|', 'n', '/\/', '^/'},
        {'0', '()', 'o', '[]', 'p'},
        {'O', 'P', 'q', '9', '(_,)'},
        {'R', '|2', 'r', '|Z', '/2'},
        {'$', '5', 's', 'z', '§'},
        {'7', '+', 't', '7'},
        {'U', '|_|', 'u', '\\_\\', '\_\_\\'},
        {'|/', 'v', '\\/'},
        {'W', '\/\/', 'w', '\\/\\/', '\^/'},
        {'%', '><', 'x', '}{'},
        {'Y', '`/', 'y', '`/', '\'/'},
        {'2', '7_', 'z', '>_', '>/'}
    };
for (i = 0; i < strlen(s), i++)
{
if (s[i] >= 'a' && s[i] < 'z' )
{
s[i] = leetMap[s[i] - 'a'];
}
if (s[i] >= 'A' && s[i] < 'Z' )
{
s[i] = leetMap[s[i] - 'A'];
}
}
return (s);
}    
