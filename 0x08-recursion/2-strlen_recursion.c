#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen_recursion - find rhw lwnghth
 * @s: skmdss
 * Return: len of string
 */
int _strlen_recursion(char *s)
{
int len;
len = strlen(s);
return (len);
}
