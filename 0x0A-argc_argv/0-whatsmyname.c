#include <stdio.h>
/**
 * main - this the main function
 * @argc: count of attributes on the command line
 * @argv: the array if string
 * Return: number
 */
int main(int argc, char *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
